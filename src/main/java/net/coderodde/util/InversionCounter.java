package net.coderodde.util;

/**
 * This interface defines the API for inversion counting algorithms. Given an
 * array {@code array}, the number of inversion in {@code array} equals the 
 * number of unique index pairs {@code i, j} ({@code i < j}) such that 
 * {@code array[i] > array[j]}.
 * 
 * @author Rodion "rodde" Efremov
 * @version 1.6 (Dec 30, 2017)
 */
public final class InversionCounter<T> {
    
//    public <T> static int count(T[] array);
}
